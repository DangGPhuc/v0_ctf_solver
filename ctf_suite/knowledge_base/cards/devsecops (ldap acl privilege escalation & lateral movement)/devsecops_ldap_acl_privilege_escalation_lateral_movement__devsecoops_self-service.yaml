id: devsecops_ldap_acl_privilege_escalation_lateral_movement__devsecoops_self-service
title: devsecoops_Self-service
category: devsecops (ldap acl privilege escalation & lateral movement)
fingerprint:
  file_types: []
  protections: []
  tags:
  - devsecops (ldap acl privilege escalation & lateral movement)
signals: []
primitive: Distilled technique from devsecoops_Self-service
preconditions: []
strategy:
- Initial access via SSH over TLS as contractor `ereid:Summer2026` on jump host.
- 'Inspected 389 Directory Server ACIs on `dc=corp,dc=nns`:'
- '`Self service` ACL allows modifying own `title`.'
- '`Contractor to permanent conversion` ACL allows group `cn=onboarding-agents` to
  `moddn` entries from `ou=contractors` to `ou=staff`. `ereid` was already in `onboarding-agents`.'
- Group `cn=helpdesk` automatically grants membership to staff with title `Platform
  Engineer` via 389-ds `automember` plugin.
- '`Helpdesk resets service account passwords` ACL allows `cn=helpdesk` to write `userPassword`
  on `uid=ops,ou=staff,dc=corp,dc=nns`.'
- 'Exploit chain:'
- 1. Changed own `title` to `Platform Engineer` via self-service `ldapmodify`.
verification:
- Check verification logic against recovered constraints
failure_modes: []
reusable_snippets: []
source:
  challenge_hash: 144a60faac635503
  learned_at: '2026-09-18T20:13:49.482393'
