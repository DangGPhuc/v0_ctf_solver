id: cryptography_lwe_crypto_light-weight_encryption
title: crypto_Light-Weight Encryption
category: cryptography (lwe
fingerprint:
  file_types:
  - '`chall.sage`, `output.py`'
  protections: []
  tags:
  - cryptography (lwe
signals:
- 'Target: `chall.sage`, `output.py`'
primitive: Distilled technique from crypto_Light-Weight Encryption
preconditions: []
strategy:
- Keygen produces $B = A s + k e \pmod q$ where $q = 2^{768}$, $A \in [0, 16)^{112
  \times 16}$, $e \in [0, r)^{112}$ ($r < 2^{32}$), $k = p / sk \pmod q$ with 520-bit
  prime $p$ and 128-bit prime $sk$.
- Left nullspace of $A$ has dimension 96. For any $u$ in nullspace, $u \cdot B \equiv
  k (u \cdot e) \pmod q$.
- Since $u \cdot e < 2^{38}$ and $q = 2^{768}$, computing ratios $(u_i \cdot B) /
  (u_0 \cdot B) \pmod q$ and rational reconstruction uniquely reveals $u_i \cdot e
  / u_0 \cdot e$.
- Common denominator reveals $u_0 \cdot e$, which directly recovers $k$.
- Extended Euclidean algorithm on $k \pmod q$ separates $p$ and $sk$.
- Finding integer nullspace of $U$ over $\mathbb{Z}$ and running CVP on $A$'s columns
  recovers the exact noise vector $e$.
- Solving linear system $A s \equiv B - k e \pmod q$ recovers secret $s$.
- Decrypting $ct = (A_I, ct_1)$ by reducing $(sk \cdot ct_1 + A_I (sk \cdot s)) \pmod
  q$ modulo $p$ reveals $pt = \text{bytes\_to\_long}(\text{flag})$.
verification:
- Check verification logic against recovered constraints
failure_modes: []
reusable_snippets: []
source:
  challenge_hash: 71728a4588878778
  learned_at: '2026-09-18T20:13:49.462029'
