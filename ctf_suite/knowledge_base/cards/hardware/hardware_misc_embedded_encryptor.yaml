id: hardware_misc_embedded_encryptor
title: misc_Embedded encryptor
category: hardware
fingerprint:
  file_types:
  - '`embedded_encryptor.jls`, `main.c`, `output.txt`'
  protections: []
  tags:
  - hardware
signals:
- 'Target: `embedded_encryptor.jls`, `main.c`, `output.txt`'
primitive: Distilled technique from misc_Embedded encryptor
preconditions: []
strategy:
- STM32 firmware runs AES-128-CBC encryption on the plaintext `havamal` with key `flag`
  and known IV.
- Between each of the 652 blocks, a 10 ms delay (`k_sleep(K_MSEC(10))`) clearly separates
  the block encryptions.
- Extracted 652 block current traces from `embedded_encryptor.jls` using `pyjls` (1500
  samples per block).
- 'Built Hamming weight power model on S-box outputs: $HW(\text{Sbox}(X_i[b] \oplus
  k))$.'
- 'Correlation Power Analysis (CPA) across all 652 traces revealed the key bytes,
  finalized by testing against the known ciphertext of block 0: `FLAG{...}`.'
- 'Status**: Solved & Submitted ✔'
- '## Postponed Challenge: `misc_Hardware accelerated flag checker 2`'
- 'Category**: Hardware / ASIC Layout Analysis'
verification:
- Check verification logic against recovered constraints
failure_modes: []
reusable_snippets: []
source:
  challenge_hash: dcb12d201cae4000
  learned_at: '2026-09-18T20:13:49.464716'
