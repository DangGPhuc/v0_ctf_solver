id: misc_misc_dyslexic
title: misc_dyslexic
category: misc
fingerprint:
  file_types: []
  protections: []
  tags:
  - misc
signals: []
primitive: Distilled technique from misc_dyslexic
preconditions: []
strategy:
- Unprivileged user `ctf` has a bash shell on serial port `/dev/ttyS0`.
- '`/challenge/flag.txt` was owned by `root:root` with mode `0600` (`-rw-------`).'
- '`/challenge` is mounted via `mount.vboxsf -o uid=0,gid=0 challenge /challenge`.'
- Due to a missing permission validation in `vboxsf_setattr`, unprivileged guest processes
  can issue `chmod 777 /challenge/flag.txt`, which passes directly through to the
  host folder and changes the mode to world-readable (`-rwxrwxrwx`).
- Read flag directly with `cat /challenge/flag.txt`.
- 'Status**: Solved & Submitted ✔'
verification:
- Check verification logic against recovered constraints
failure_modes: []
reusable_snippets: []
source:
  challenge_hash: 79dd781177f7fdcf
  learned_at: '2026-09-18T20:13:49.461208'
